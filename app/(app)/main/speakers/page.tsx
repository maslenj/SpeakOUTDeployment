import { LogoutButton } from "@/components/Auth";

export default async function Speakers() {
  return (
    <>
      <main>
        this is the speakers page (it's not done yet)
        <div>
          secret logout button: <LogoutButton />
        </div>
      </main>
    </>
  )
}